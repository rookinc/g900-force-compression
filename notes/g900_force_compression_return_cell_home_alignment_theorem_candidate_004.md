# G900 force compression return-cell home-alignment theorem candidate 004

Status: theorem candidate recorded

Statement

For the admitted return-cell one-step information transport, the source
support maps exactly into the admitted target-home slot set.

Under the conservative target-home proxy, the transport reduces mean
distance to home from 2.75 to 0.0 and support radius from 5 to 0 while
preserving support size and entropy.

Compression subtype

homeward alignment compression

Negative controls

The same transport does not compress toward the source-home set or the
six-nine pair under the tested metrics.

The full return-cell slot set is exact but non-discriminating because it
contains both source and target support.

Boundary

The theorem candidate is local to the admitted return-cell one-step
information transport.

It does not prove a global compression shadow.
It does not admit physical gravity.
It does not admit a force renderer.
It does not mutate the body.
